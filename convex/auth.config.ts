const autoConfig = {
    providers: [
        {
            domain: "https://teaching-beetle-60.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};

export default autoConfig;